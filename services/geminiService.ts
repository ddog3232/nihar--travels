import { TripPlanRequest } from "../types";

export const generateTripPlan = async (request: TripPlanRequest): Promise<string> => {
  return "AI Planning is currently disabled. Please contact us directly for a custom itinerary.";
};

